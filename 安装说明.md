# 安装和运行说明

## 第一步：安装依赖

打开终端，进入项目目录：

```bash
cd "/Users/mima0000/CodeBuddy/客户管理系统"
npm install
```

## 第二步：初始化数据库

```bash
# 生成 Prisma Client
npx prisma generate

# 推送数据库 schema（创建 SQLite 数据库和表）
npx prisma db push
```

## 第三步：创建上传目录

```bash
mkdir -p public/uploads
```

## 第四步：启动开发服务器

```bash
npm run dev
```

然后在浏览器中访问：http://localhost:3000

## 数据库管理（可选）

如果你想通过图形界面管理数据库：

```bash
npx prisma studio
```

这会打开一个网页界面，你可以在这里查看和编辑数据。

## 项目功能

### 1. 仪表盘（首页）
- 显示客户、产品、供应商、订单的统计数量
- 显示总收入
- 快速操作按钮
- 最近订单列表
- 库存预警

### 2. 客户管理
- 客户列表（支持搜索、筛选、分页）
- 新增客户
- 编辑客户
- 删除客户
- 查看客户详情（包含订单历史和关联文件）

### 3. 产品管理
- 产品列表（支持搜索、分类筛选、库存预警）
- 新增产品
- 编辑产品
- 删除产品
- 查看产品详情（包含订单历史和供应商信息）

### 4. 供应商管理
- 供应商列表（支持搜索、筛选）
- 新增供应商
- 编辑供应商
- 删除供应商
- 查看供应商详情（包含供应产品列表）

### 5. 订单管理
- 订单列表（支持搜索、状态筛选）
- 创建订单（选择客户、添加产品、自动计算金额）
- 查看订单详情
- 删除订单（自动恢复库存）

### 6. 文件管理
- 文件列表（支持搜索）
- 文件上传（可关联到客户、供应商、订单或产品）
- 文件下载
- 文件删除

## 技术栈

- **框架**: Next.js 14 (App Router)
- **语言**: TypeScript
- **样式**: Tailwind CSS
- **数据库**: SQLite
- **ORM**: Prisma
- **UI**: Radix UI + 自定义组件

## 注意事项

1. 数据库文件位于 `prisma/dev.db`
2. 上传的文件存储在 `public/uploads/` 目录
3. 可在 `.env` 文件中修改配置
4. 开发模式下支持热更新

## 常见问题

### Q: 端口 3000 被占用怎么办？
A: 修改 `package.json` 中的 `dev` 脚本，添加 `-p 3001` 参数：
```json
"dev": "next dev -p 3001"
```

### Q: 如何重置数据库？
A: 删除 `prisma/dev.db` 文件，然后重新运行 `npx prisma db push`

### Q: 如何查看 API？
A: 所有 API routes 位于 `src/app/api/` 目录，可以通过浏览器或 Postman 测试。

## 下一步

建议你：
1. 先运行起来看看效果
2. 通过 Prisma Studio 添加一些测试数据
3. 根据需要修改样式和功能
4. 添加用户认证和权限管理
5. 添加数据统计图表
