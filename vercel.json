{
  "buildCommand": "sed -i 's/provider = \"sqlite\"/provider = \"postgresql\"/' prisma/schema.prisma && npx prisma db push --accept-data-loss --skip-generate && npx prisma generate && next build",
  "installCommand": "npm install",
  "framework": "nextjs"
}
